<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--<item name="colorAccent">@color/black</item>-->
        <item name="android:windowActionBarOverlay">false</item>
<!---->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!--theme of status bar-->
    <style name="AppTheme.statusbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimaryDark">@color/colorPrimary</item>
    </style>
    <!--tab customizations-->
    <style name="TabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">12sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!--Custom Edittext theme-->
    <style name="CustomEditTextStyle" parent="android:Widget.EditText">

        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item>
        <!--<item name="android:color"></item>-->
        <item name="android:backgroundTint">@color/bg_colorinput</item>
        <item name="android:textSize">16sp</item>
        <item name="android:drawableRight">@drawable/ic_tick_green</item>
        <item name="android:drawablePadding">@dimen/normal_padding</item>
        
        <!-- ... More items here if needed ... -->
    </style>
    <style name="App_Theme" parent="App_Theme">
        <item name="android:editTextStyle">@style/CustomEditTextStyle</item>
        <item name="editTextStyle">@style/CustomEditTextStyle</item> <!-- For compatibility with the support library -->
    </style>
     <!--Seek bar theme-->
    <style name="MySeekBar" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressBackgroundTint">@color/dark_grey</item>
        <item name="android:progressTint">@color/colorPrimary</item>
        <item name="android:colorControlActivated">@color/colorPrimary</item>
    </style>


</resources>
